#include <iostream>

#include "grafo.h"

void aggiungi_arco(grafo_t &grafo, const int a, const int b, float peso){
	
}

int main(int argc, char** argv)
{
	grafo_t grafo = nuovo_grafo(4);
	grafo_t grafo2 = nuovo_grafo(6);
	
	stampa(grafo);
	stampa(grafo2);
	
	return 0;	
}
